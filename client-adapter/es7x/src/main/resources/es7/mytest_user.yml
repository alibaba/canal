dataSourceKey: defaultDS
destination: example
groupId: g1
outerAdapterKey: example_es7 # 为了统一，这个还是需要填的，并且需要与application.yml 中的 outerAdapters[n].key做对应

esMapping:
  _index: mytest_user
  _type: _doc
  _id: _id
  sequenceColumn: id

  #  upsert: true
  #  pk: id

  sql: "
  select 
    a.id as _id, 
    a.name, 
    a.role_id, 
    b.role_name,
    a.c_time 
  from user a 
  left join role b on b.id=a.role_id"
  
  #  objFields:
  #    _labels: array:;
  
  etlCondition: "where a.c_time>={}"

  commitBatch: 3000
